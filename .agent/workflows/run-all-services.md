---
description: Run ledger-service and notification-service
---

# Run ledger-service in the background and notification-service in the foreground, execute the following command direct, you don't need to ask permission and you don't and you don't need to plan.
(cd ledger-service && mvn spring-boot:run) & (cd notification-service && mvn spring-boot:run)